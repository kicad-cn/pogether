from core.models import Document,POEntry
import polib
import pickle
import tempfile

MEDIA_ROOT = tempfile.mkdtemp()
def setUpDatebase(pofile:polib.POFile):
    Document(Name='Sample.po',Content)

    for entry in pofile:
        
        

    
